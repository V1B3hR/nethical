# Istio DestinationRule for Asia Pacific Northeast 1 Region (Tokyo)
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: nethical-api-ap-northeast-1
  namespace: nethical
  labels:
    region: ap-northeast-1
spec:
  host: nethical-api
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 1000
        connectTimeout: 5s
      http:
        h2UpgradePolicy: UPGRADE
        http1MaxPendingRequests: 1000
        http2MaxRequests: 1000
        maxRequestsPerConnection: 100
        maxRetries: 3
    loadBalancer:
      simple: LEAST_REQUEST
      localityLbSetting:
        enabled: true
        failoverPriority:
          - "topology.kubernetes.io/region"
          - "topology.kubernetes.io/zone"
    outlierDetection:
      consecutive5xxErrors: 5
      interval: 10s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
    - name: ap-northeast-1
      labels:
        region: ap-northeast-1
    - name: apac-fallback
      labels:
        region-role: secondary
