# Production dependency input file (no hashes). Generated/maintained manually.
bcrypt==5.0.0
argon2-cffi==25.1.0
# Lock file: requirements-hashed.txt (generated by pip-compile).
# Update process:
#   1. Edit versions here.
#   2. Run: pip-compile --generate-hashes --output-file requirements-hashed.txt requirements.txt
#   3. Commit both files.
#
# To regenerate the lock file locally:
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file requirements-hashed.txt requirements.txt

# Web server framework (optional - for API server)
fastapi==0.141.1
uvicorn==0.52.3
# Note: sse-starlette was removed due to incompatible starlette version requirements.
# fastapi is updated to 0.121.0 which uses starlette 0.40.0+ (avoiding CVE in < 0.40.0).
# SSE functionality is not currently used in the codebase.

# Data validation and serialization
pydantic==2.13.4
pydantic-core==2.48.0
typing-extensions==4.16.0
dataclasses-json==0.6.7

# Scientific computing and data processing
numpy>=2.4.5
pandas>=3.0.3

# Configuration and data formats
PyYAML==6.0.3
jsonschema>=4.19.0,<5.0.0

# Security
cryptography>=50.0.0
defusedxml==0.7.1
PyJWT>=2.0.0
passlib[bcrypt]>=1.7.4

# Database
sqlalchemy>=2.0.52
alembic>=1.19.1    

# Transitive/supporting (explicitly pinned to keep lock stable)
annotated-types==0.8.0
marshmallow==3.26.2
typing-inspect==0.9.0
typing-inspection==0.4.4
mypy-extensions==1.1.0
python-dateutil==2.9.0.post0
pytz==2024.2
tzdata==2025.3
# Pin starlette to the range fastapi 0.121.0 supports for reproducibility and security.
# This explicit pin ensures builds remain consistent even if fastapi's range allows
# multiple versions. Update this when updating fastapi.
starlette>=1.6.0,<1.7.0

# LLM Integration SDKs (optional - for specific integrations)
anthropic>=0.122.0  # For Claude/Anthropic integration

# Optional LangChain integration (uncomment if needed; then re-run pip-compile)
# langchain>=0.1.0
# langchain-core>=0.1.0

# Monitoring and observability
prometheus-client>=0.26.0  # Prometheus metrics export
aiohttp>=3.14.3  # Async HTTP for metrics server and webhooks

# Optional performance dependencies (uncomment if needed; then re-run pip-compile)
# redis==4.0.0
# psycopg2-binary==2.9.0
# elasticsearch==8.0.0
# numba==0.56.0
# torch==2.0.0
# py-spy>=0.3.14  # Optional: For production profiling with flamegraphs
